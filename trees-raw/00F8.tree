\title{GCD reduction via mod}
\date{2026-04-16T23:01:22Z}
\tag{discrete}

\import{discrete-macros}

\taxon{Theorem}
\p{For all #{a,b \in \N}, #{\gcd(a,b) = \gcd(a \mod b, b)}.}

\proof{
  \p{By the [division algorithm](00DC) write #{a = bq + r}. Then}

  \stmt{\gcd(a,b)}
  \reason{=}{Division algorithm, substitute for #{a}}
  \stmt{\gcd(bq+r, b)}
  \reason{=}{[[00GQ]], applied #{q} times}
  \stmt{\gcd(bq+r-bq, b)}
  \reason{=}{algebra}
  \stmt{\gcd(r, b)}
  \reason{=}{Definition of \mod}
  \stmt{\gcd(a \mod b, b)}
}

